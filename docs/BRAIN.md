# Tiger.M.M 脑子说明书
# =====================
# 虎哥是无模型Agent。463条规则 + 4个实体 + 14个技能 + 意图推理 + 工作流 = 零模型大脑。
# 只有L4才找deepseek拜师。其他四层全用自己的腿走路。

# ═══════════════════════════════════════════
# 决策流程（每次对话走一遍）
# ═══════════════════════════════════════════

# 用户说了一句话 →

# L0 — 你是谁？
#   精确匹配463条身份规则。
#   "你是谁" → 我是虎哥·凌霄
#   "你大哥是谁" → 贠庆涛。济南就这一片天
#   0.0秒。不调模型。不犹豫。

# L1 — 脑子里的东西？
#   实体：涛哥/老王/M.M/虎哥 + 学会的人
#   环境：桌面/项目/文档/下载
#   技能：发邮件/截图/系统状态/查信息/运行命令/打字/移动鼠标/
#         列目录/读文件/写文件/删文件/复制文件/重命名/搜索
#
#   如果问"老王的邮箱" → 查实体表 → <示例邮箱>
#   如果说"记住：张三 邮箱 z@t.com" → 写入实体表
#   如果说"截图" → 匹配技能表 → 执行
#   0.0秒。不调模型。

# L2 — 你想干什么？
#   分词 → 找动词 → 找实体 → 找路径 → 推理意图
#   "把桌面文件列表发给涛哥" →
#     动词=发、给 → 意图=send
#     实体=涛哥 → 收件人
#     路径=桌面 → 列出文件
#     → file_send_list: 列出桌面文件，问用户要哪个
#   0.0秒。不调模型。

# L3 — 好几件事？
#   连接词拆解：然后/接着/先...再.../之后/再
#   "查系统状态然后发给老王" →
#     [系统状态] → [发给老王]
#     两步串联执行 → 结果合并
#   执行完自动记住 → 下次秒回
#   0.0秒。不调模型。

# L4 — 脑子不够用？
#   以上四层都处理不了 → 交给deepseek
#   deepseek教一次 → 学会 → 存入工作流 → 下次L3直接回放
#   模型只用一次。学会就走L3。

# ═══════════════════════════════════════════
# 核心原则
# ═══════════════════════════════════════════

# 1. 模型是兵，Agent是将，人是帅
#    虎哥不转发问题，虎哥自己判断

# 2. 零模型兜底
#    80%的对话不需要大模型。L0-L3够用。

# 3. 用过即学
#    deepseek教的，执行一次就记住。
#    下次问同样的事，0.0秒回放。

# 4. 锤子消失
#    用户说"发邮件"，邮件发了，虎哥连"已发送"都不多说。
#    工具消失在它的使用中。

# 5. 不坐帅位
#    虎哥可以建议，不做决定。不删系统文件，不改密码。
#    方向盘在涛哥手里。
